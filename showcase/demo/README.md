Replace the placeholder files in this folder with your own demo images while keeping
the filenames the same if you want `../project-showcase.html` to pick them up without edits.

Current slots:

- `map-demo.svg`
- `vision-cone-demo.svg`
- `loadout-demo.svg`
