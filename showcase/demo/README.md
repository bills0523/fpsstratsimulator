Replace the placeholder files in this folder with your own demo images while keeping
the filenames the same if you want `../../index.html` to pick them up without edits.

Current slots:

- `mao-demo.png`
- `vision-cone-demo.jpg`
- `loadout-demo.svg`
